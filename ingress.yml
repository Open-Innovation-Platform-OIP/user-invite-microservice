apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: oip-invite-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-staging"
spec:
  tls:
  - hosts:
    - oip-invite.k8s.jaagalabs.com
    secretName: oip-invite-tls
  rules:  
  - host: oip-invite.k8s.jaagalabs.com
    http:
      paths:
      - backend:
          serviceName: oip-invite
          servicePort: 80