apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: oip-invite-dev-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - oip-invite-dev.k8s.jaagalabs.com
    secretName: oip-invite-dev-tls
  rules:  
  - host: oip-invite-dev.k8s.jaagalabs.com
    http:
      paths:
      - backend:
          serviceName: oip-invite-dev
          servicePort: 80