apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: oip-invite-dev-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - oip-invite-dev.oip.dev.code-alpha.org
    secretName: oip-invite-dev-tls
  rules:  
  - host: oip-invite-dev.oip.dev.code-alpha.org
    http:
      paths:
      - backend:
          serviceName: oip-invite-dev
          servicePort: 80